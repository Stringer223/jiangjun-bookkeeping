node_modules
dist
out
*.log
.DS_Store

# AI 工具的工作目录，不是项目代码
.workbuddy/

# 环境变量文件：绝不允许提交（可能含密钥）。示例文件除外，它是给人抄的模板
.env
.env.*
!.env.example

# 本地账本数据。正常情况下它存在 %APPDATA%\将军记账\ 下，
# 这里兜底：调试时若把 userData 指到项目里，也不会把账本或留证文件误提交
data.json
data.json.*

# Claude Code 的机器本地状态。.claude/ 本身要进版本库（gitcommit-agent 是
# 有意提交的），但这个文件是每台机器各一份的本地覆盖，不该共享
.claude/settings.local.json

# Python 字节码缓存。门禁的判官脚本是 .py，只要有人 import 它（跑测试、排查问题）
# 就会生成这个目录；不挡的话 git add -A 会把 .pyc 扫进提交
__pycache__/
*.pyc
